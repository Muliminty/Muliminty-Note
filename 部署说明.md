# GitHub Pages 部署说明

本笔记使用 [Quartz](https://quartz.jzhao.xyz/) 生成静态网站并部署到 GitHub Pages。

## ✨ 特性

- ✅ **完美支持 Obsidian 双链**：`[[wikilink]]` 和 `[文本](./路径)` 格式都支持
- ✅ **自动部署**：推送到 master 分支后自动构建和部署
- ✅ **美观的界面**：现代化的搜索、导航和主题
- ✅ **响应式设计**：支持移动端访问
- ✅ **快速搜索**：全文搜索功能

## 🚀 快速开始

### 1. 安装依赖

```bash
npm install
```

### 2. 本地预览

```bash
npm run dev
```

然后在浏览器中打开 `http://localhost:8080` 查看效果。

### 3. 构建静态文件

```bash
npm run build
```

构建后的文件会在 `public/` 目录中。

## 📦 部署到 GitHub Pages

### 步骤 1：启用 GitHub Pages

1. 进入 GitHub 仓库的 **Settings** → **Pages**
2. 在 **Source** 中选择 **GitHub Actions**
3. 保存设置

### 步骤 2：推送代码

```bash
git add .
git commit -m "Add Quartz configuration"
git push origin master
```

### 步骤 3：等待自动部署

GitHub Actions 会自动：
1. 构建 Quartz 网站
2. 部署到 GitHub Pages

部署完成后，访问：`https://你的用户名.github.io/Muliminty-Note/`

## 🔧 配置说明

### 修改网站标题和描述

编辑 `quartz.config.ts` 文件：

```typescript
configuration: {
  pageTitle: "你的网站标题",
  baseUrl: "你的用户名.github.io",
  // ...
}
```

### 排除文件/目录

在 `quartz.config.ts` 的 `ignorePatterns` 中添加：

```typescript
ignorePatterns: [
  "private",
  "xx-归档",
  "xx-草稿",
  // 添加你不想发布的文件
],
```

### 自定义域名

1. 创建 `content/CNAME` 文件，内容为你的域名：
   ```
   yourdomain.com
   ```

2. 在 GitHub 仓库设置中添加自定义域名

## 📝 Obsidian 双链支持

Quartz 原生支持以下 Obsidian 双链格式：

### Wikilink 格式
```markdown
[[文件名]]
[[文件名|显示文本]]
[[文件名#锚点]]
```

### Markdown 链接格式
```markdown
[显示文本](./文件名.md)
[显示文本](./文件名.md#锚点)
```

两者都会被正确解析并生成链接！

## 🎨 自定义主题

编辑 `quartz.config.ts` 中的 `theme` 配置：

```typescript
theme: {
  colors: {
    lightMode: {
      // 自定义浅色主题颜色
    },
    darkMode: {
      // 自定义深色主题颜色
    },
  },
}
```

## 📚 更多资源

- [Quartz 官方文档](https://quartz.jzhao.xyz/)
- [Quartz 配置选项](https://quartz.jzhao.xyz/configuration)
- [Quartz 插件列表](https://quartz.jzhao.xyz/plugins)

## ❓ 常见问题

### Q: 为什么某些文件没有显示？

A: 检查 `quartz.config.ts` 中的 `ignorePatterns`，确保文件没有被排除。

### Q: 如何更新 Quartz？

A: 运行 `npm update quartz` 更新到最新版本。

### Q: 本地预览正常，但部署后有问题？

A: 检查 GitHub Actions 日志，查看构建错误信息。

---

**提示**：首次部署可能需要几分钟时间，请耐心等待。

