# 快速开始指南

## 🚀 一键初始化（推荐）

最简单的方式是使用 Quartz 的 CLI 工具初始化：

```bash
# 安装 Quartz CLI（全局安装）
npm install -g quartz-cli

# 在项目根目录初始化（会保留现有文件）
npx quartz create
```

## 📦 手动安装

如果自动初始化失败，可以手动安装：

```bash
# 1. 安装依赖
npm install

# 2. 本地预览（会自动创建必要的目录结构）
npm run dev
```

## ⚙️ 配置说明

### 1. 修改网站信息

编辑 `quartz.config.ts`：

```typescript
configuration: {
  pageTitle: "你的笔记标题",
  baseUrl: "你的用户名.github.io",  // 或你的自定义域名
  // ...
}
```

### 2. 排除不需要发布的文件

在 `ignorePatterns` 中添加：

```typescript
ignorePatterns: [
  "xx-归档",
  "xx-草稿",
  ".obsidian",
  // 添加其他要排除的目录
],
```

## 🎯 首次部署

1. **提交配置到 Git**：
   ```bash
   git add .
   git commit -m "Add Quartz configuration"
   git push origin master
   ```

2. **在 GitHub 启用 Pages**：
   - 进入仓库 Settings → Pages
   - Source 选择 "GitHub Actions"
   - 保存

3. **等待自动部署**：
   - GitHub Actions 会自动构建并部署
   - 首次部署需要 3-5 分钟
   - 完成后访问：`https://你的用户名.github.io/Muliminty-Note/`

## 🔗 Obsidian 双链支持

Quartz 完美支持以下格式：

### Wikilink 格式
```markdown
[[文件名]]
[[文件名|显示文本]]
[[文件夹/文件名]]
[[文件名#锚点]]
```

### Markdown 链接格式
```markdown
[显示文本](./文件名.md)
[显示文本](./文件夹/文件名.md)
[显示文本](./文件名.md#锚点)
```

**注意**：如果使用 wikilink `[[]]` 格式，需要确保文件扩展名正确（`.md`）。

## 📝 本地开发

```bash
# 启动开发服务器（支持热更新）
npm run dev

# 构建静态文件
npm run build

# 预览构建结果
npm run preview
```

## ❓ 遇到问题？

### 问题：找不到 quartz/cfg 或 quartz/plugins

**解决方案**：
```bash
# 删除 node_modules 和 package-lock.json
rm -rf node_modules package-lock.json

# 重新安装
npm install
```

### 问题：构建失败

1. 检查 Node.js 版本（需要 18+）：
   ```bash
   node --version
   ```

2. 查看详细错误：
   ```bash
   npm run build -- --verbose
   ```

### 问题：某些文件没有显示

检查 `ignorePatterns` 配置，确保文件没有被排除。

## 📚 更多资源

- [Quartz 官方文档](https://quartz.jzhao.xyz/)
- [Quartz GitHub](https://github.com/jackyzha0/quartz)
- [部署说明](./部署说明.md)

