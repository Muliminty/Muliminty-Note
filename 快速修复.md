# 快速修复：目录和双链预览不显示

## 🔍 问题诊断

如果部署后网站可以访问，但左侧目录和双链预览不显示，请按以下步骤操作：

## ✅ 步骤 1：检查配置文件

确保以下文件存在且内容正确：

### 1.1 检查文件是否存在

```bash
ls -la quartz.config.ts quartz.layout.ts
```

应该看到两个文件都存在。

### 1.2 检查 `quartz.config.ts`

确保文件中有以下内容：

```typescript
import * as Shared from "./quartz.layout"

const config: QuartzConfig = {
  configuration: {
    enablePopovers: true,  // ← 必须启用
    // ...
  },
  layout: {
    sharedPageComponents: Shared.sharedPageComponents,
    defaultContentPageLayout: Shared.defaultContentPageLayout,
    defaultListPageLayout: Shared.defaultListPageLayout,
  },
}
```

### 1.3 检查 `quartz.layout.ts`

确保文件中有以下导出：

```typescript
export const sharedPageComponents: SharedLayout = {
  left,
  right,
  header,
  footer,
}

export const defaultContentPageLayout: PageLayout = {
  // ...
}

export const defaultListPageLayout: PageLayout = {
  // ...
}
```

## ✅ 步骤 2：本地测试

### 2.1 安装依赖

```bash
npm install
```

### 2.2 清理缓存

```bash
rm -rf .quartz-cache/ public/
```

### 2.3 构建并测试

```bash
npm run build
npm run dev
```

### 2.4 检查本地效果

访问 `http://localhost:8080`，检查：
- ✅ 左侧边栏是否显示搜索框
- ✅ 左侧边栏是否显示目录浏览器
- ✅ 鼠标悬停在双链上是否有预览

**如果本地正常**：继续步骤 3

**如果本地也不正常**：检查配置文件语法，查看错误信息

## ✅ 步骤 3：提交并推送

### 3.1 确保文件已提交

```bash
# 检查文件状态
git status

# 如果文件未提交，添加并提交
git add quartz.config.ts quartz.layout.ts
git commit -m "Fix layout configuration"
git push origin master
```

### 3.2 等待自动部署

GitHub Actions 会自动构建和部署，等待 3-5 分钟。

### 3.3 检查部署结果

访问网站，检查功能是否正常。

## ✅ 步骤 4：如果问题仍然存在

### 4.1 检查 GitHub Actions 日志

1. 进入 GitHub 仓库的 **Actions** 标签页
2. 查看最新的构建日志
3. 检查是否有错误信息

### 4.2 检查浏览器控制台

1. 打开网站
2. 按 F12 打开开发者工具
3. 查看 Console 标签是否有错误
4. 查看 Network 标签，检查资源是否加载成功

### 4.3 强制重新构建

```bash
# 创建一个空提交来触发重新构建
git commit --allow-empty -m "Trigger rebuild"
git push origin master
```

## 🔧 常见问题

### 问题 1：配置文件未提交

**症状**：GitHub Actions 构建成功，但功能不显示

**解决**：
```bash
git add quartz.config.ts quartz.layout.ts
git commit -m "Add layout configuration"
git push origin master
```

### 问题 2：依赖版本不匹配

**症状**：构建时出现错误

**解决**：
```bash
rm -rf node_modules package-lock.json
npm install
git add package-lock.json
git commit -m "Update dependencies"
git push origin master
```

### 问题 3：浏览器缓存

**症状**：本地正常，但部署后不正常

**解决**：
1. 清除浏览器缓存
2. 硬刷新（Ctrl/Cmd + Shift + R）
3. 或使用无痕模式访问

## 📝 验证清单

修复后，检查以下项目：

- [ ] 配置文件存在且正确
- [ ] `enablePopovers: true` 已设置
- [ ] `layout` 配置已添加
- [ ] `quartz.layout.ts` 正确导出
- [ ] 文件已提交到 Git
- [ ] GitHub Actions 构建成功
- [ ] 本地测试正常
- [ ] 部署后功能正常

## 🚨 如果仍然无法解决

1. **查看 Quartz 官方文档**：
   - https://quartz.jzhao.xyz/configuration

2. **检查 GitHub Issues**：
   - https://github.com/jackyzha0/quartz/issues

3. **提交 Issue**：
   - 在 GitHub 上提交 Issue，附上配置文件内容和错误日志

