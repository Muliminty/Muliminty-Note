# 微信长按翻译

```javaScript
const txt1 = (str) => {
  const perStr = '长按翻译';
  const tranStr = [...str].reduce((a, _, index) => {
    return `${a}\\u${str.charCodeAt(index).toString(16)}`
  })

  return perStr + tranStr

}

let str = '小公鸡点到谁'
txt1(str)

console.log(txt1(str))
```
