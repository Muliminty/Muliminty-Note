# Quartz 笔记部署完整指南

> 本指南涵盖从零开始搭建 Quartz 笔记网站到日常维护的完整流程

---

## 📋 目录

- [一、从零开始搭建](#一从零开始搭建)
- [二、笔记文件管理](#二笔记文件管理)
- [三、本地调试](#三本地调试)
- [四、部署到 GitHub Pages](#四部署到-github-pages)
- [五、更新笔记](#五更新笔记)
- [六、触发重新构建](#六触发重新构建)
- [七、配置说明](#七配置说明)
- [八、常见问题](#八常见问题)

---

## 一、从零开始搭建

### 1.1 创建 GitHub 仓库

1. **登录 GitHub**，点击右上角 **+** → **New repository**

2. **填写仓库信息**：
   - **Repository name**：`Muliminty-Note`（或你的仓库名）
   - **Description**：可选，描述你的笔记
   - **Visibility**：选择 **Public**（公开）或 **Private**（私有）
   - 勾选 **Add a README file**

3. **点击 Create repository**

### 1.2 克隆仓库到本地

```bash
# 克隆仓库
git clone https://github.com/你的用户名/Muliminty-Note.git
cd Muliminty-Note
```

### 1.3 初始化 Quartz 项目

#### 方法一：使用 Quartz CLI（推荐）

```bash
# 安装 Quartz CLI（全局安装）
npm install -g quartz-cli

# 在项目根目录初始化
npx quartz create
```

#### 方法二：手动安装

1. **创建 `package.json`**：

```json
{
  "name": "muliminty-note",
  "version": "1.0.0",
  "description": "全栈开发知识体系学习笔记",
  "scripts": {
    "dev": "quartz build --serve",
    "build": "quartz build",
    "preview": "quartz preview"
  },
  "dependencies": {
    "@quartz/plugin-aliasredirects": "^4.4.0",
    "@quartz/plugin-cname": "^4.4.0",
    "@quartz/plugin-content": "^4.4.0",
    "@quartz/plugin-emoji": "^4.4.0",
    "@quartz/plugin-explicitlinks": "^4.4.0",
    "@quartz/plugin-folder": "^4.4.0",
    "@quartz/plugin-git": "^4.4.0",
    "@quartz/plugin-giscus": "^4.4.0",
    "@quartz/plugin-graph": "^4.4.0",
    "@quartz/plugin-latex": "^4.4.0",
    "@quartz/plugin-mermaid": "^4.4.0",
    "@quartz/plugin-obsidian": "^4.4.0",
    "@quartz/plugin-pagefolders": "^4.4.0",
    "@quartz/plugin-search": "^4.4.0",
    "@quartz/plugin-tag": "^4.4.0",
    "@quartz/plugin-toc": "^4.4.0",
    "@quartz/plugin-wikilinks": "^4.4.0",
    "quartz": "^4.4.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0"
  }
}
```

2. **安装依赖**：

```bash
npm install
```

3. **创建配置文件**：
   - 创建 `quartz.config.ts`（主配置文件）
   - 创建 `quartz.layout.ts`（布局配置文件）

### 1.4 配置 GitHub Actions

创建 `.github/workflows/deploy.yml`：

```yaml
name: Deploy Quartz site to GitHub Pages

on:
  push:
    branches:
      - master
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Install Dependencies
        run: npm ci
      - name: Build Quartz site
        run: npm run build
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: public

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
```

### 1.5 配置 `.gitignore`

创建或更新 `.gitignore`：

```gitignore
# Quartz
.quartz-cache/
public/

# Node
node_modules/
package-lock.json
yarn.lock

# Obsidian
.obsidian/

# macOS
.DS_Store

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
```

### 1.6 提交并推送

```bash
# 添加所有文件
git add .

# 提交
git commit -m "Initial commit: Add Quartz configuration"

# 推送到 GitHub
git push origin master
```

### 1.7 启用 GitHub Pages

1. 进入 GitHub 仓库的 **Settings** → **Pages**
2. 在 **Source** 中选择 **GitHub Actions**
3. 保存设置

### 1.8 等待自动部署

GitHub Actions 会自动：
1. 构建 Quartz 网站
2. 部署到 GitHub Pages

首次部署需要 **3-5 分钟**，完成后访问：`https://你的用户名.github.io/Muliminty-Note/`

---

## 二、笔记文件管理

### 2.1 文件组织结构

```
项目根目录/
├── 00-前端体系/          # 笔记内容目录
│   ├── !MOC-前端体系.md  # MOC（目录）文件
│   └── 01-基础入门/
│       └── ...
├── 00-经验技巧/
├── 03-服务端/
├── xx-归档/              # 归档目录（不会发布）
├── xx-草稿/              # 草稿目录（不会发布）
├── .obsidian/            # Obsidian 配置（不会发布）
├── quartz.config.ts      # Quartz 配置文件
├── quartz.layout.ts      # 布局配置文件
└── package.json          # 项目依赖
```

### 2.2 文件命名规范

- **MOC 文件**：`!MOC-分类名.md`（如 `!MOC-前端体系.md`）
- **普通文件**：使用中划线分隔（如 `变量声明.md`）
- **目录名**：使用数字前缀+中划线+名称（如 `01-语言核心`）

### 2.3 文件夹管理规则

#### 📁 哪些文件夹会在部署后展示？

**✅ 会展示的文件夹**：
- 所有以数字开头的文件夹（如 `00-前端体系`、`01-计算机基础`、`03-服务端`）
- 任何包含 `.md` 文件的文件夹
- 任何不在 `ignorePatterns` 中的文件夹

**示例**：
```
✅ 00-前端体系/          # 会展示
✅ 00-经验技巧/          # 会展示
✅ 01-计算机基础/        # 会展示
✅ 02-代码管理/          # 会展示
✅ 03-服务端/            # 会展示
✅ 书籍/                 # 会展示（如果包含 .md 文件）
```

#### ❌ 哪些文件夹不会在部署后展示？

**❌ 不会展示的文件夹**：
- 在 `ignorePatterns` 中配置的文件夹
- 以 `.` 开头的隐藏文件夹（如 `.obsidian`、`.git`）
- 系统文件夹（如 `node_modules`）

**默认不展示的文件夹**：
```
❌ xx-归档/              # 归档目录（不会发布）
❌ xx-草稿/              # 草稿目录（不会发布）
❌ private/              # 私有文件（不会发布）
❌ .obsidian/            # Obsidian 配置（不会发布）
❌ node_modules/         # Node 依赖（不会发布）
❌ .git/                 # Git 配置（不会发布）
❌ .quartz-cache/        # Quartz 缓存（不会发布）
❌ public/               # 构建输出目录（不会发布）
```

#### 📋 文件夹命名规范

**推荐命名方式**：
1. **使用数字前缀**：便于排序和分类
   ```
   00-前端体系/
   01-计算机基础/
   02-代码管理/
   03-服务端/
   ```

2. **使用描述性名称**：清晰表达文件夹用途
   ```
   ✅ 00-前端体系/
   ✅ 01-基础入门/
   ✅ 02-框架进阶/
   
   ❌ 文件夹1/
   ❌ 临时/
   ❌ 未分类/
   ```

3. **使用中划线分隔**：提高可读性
   ```
   ✅ 00-前端体系/
   ✅ 01-基础入门/
   
   ❌ 00_前端体系/
   ❌ 00.前端体系/
   ```

#### 🔧 如何控制文件夹的展示？

**方法一：使用 ignorePatterns（推荐）**

在 `quartz.config.ts` 中配置：

```typescript
ignorePatterns: [
  "private",           // 私有文件
  "xx-归档",           // 归档目录（不会发布）
  "xx-草稿",           // 草稿目录（不会发布）
  ".obsidian",         // Obsidian 配置（不会发布）
  "node_modules",      // Node 依赖（不会发布）
  ".git",              // Git 配置（不会发布）
  "temp",              // 临时文件夹（不会发布）
  "draft",             // 草稿文件夹（不会发布）
],
```

**方法二：使用文件夹命名约定**

建议使用以下命名约定：
- `xx-*`：不会发布的文件夹（如 `xx-归档`、`xx-草稿`）
- `private`：不会发布的私有文件夹
- 数字开头的文件夹：会发布的文件夹（如 `00-前端体系`）

**方法三：删除或移动文件夹**

如果不想发布某个文件夹：
1. 移动到 `xx-归档` 或 `xx-草稿` 目录
2. 或添加到 `ignorePatterns` 中
3. 或删除该文件夹

#### 📝 文件夹组织建议

**推荐的文件夹结构**：

```
项目根目录/
├── 00-前端体系/          ✅ 会展示
│   ├── 01-基础入门/
│   ├── 02-框架进阶/
│   └── ...
├── 00-经验技巧/          ✅ 会展示
│   └── 收集箱/
├── 01-计算机基础/        ✅ 会展示
├── 02-代码管理/          ✅ 会展示
├── 03-服务端/            ✅ 会展示
│   ├── 01-服务端语言/
│   ├── 02-Web服务器/
│   └── ...
├── 书籍/                 ✅ 会展示（如果包含 .md 文件）
├── xx-归档/              ❌ 不会展示
├── xx-草稿/              ❌ 不会展示
├── .obsidian/            ❌ 不会展示
└── node_modules/         ❌ 不会展示
```

**最佳实践**：
1. **使用数字前缀**：便于排序和分类
2. **清晰的命名**：使用描述性的文件夹名称
3. **统一结构**：保持文件夹结构的一致性
4. **分类管理**：将相关笔记放在同一文件夹下
5. **使用 MOC 文件**：在每个文件夹下创建 MOC 文件作为索引

#### ⚠️ 注意事项

1. **文件夹名称区分大小写**：
   ```
   ✅ 00-前端体系/
   ❌ 00-前端体系/  （注意大小写）
   ```

2. **避免使用特殊字符**：
   ```
   ✅ 00-前端体系/
   ❌ 00-前端体系(备份)/
   ❌ 00-前端体系@/
   ```

3. **空文件夹不会展示**：
   - 如果文件夹中没有 `.md` 文件，即使不在 `ignorePatterns` 中也不会展示
   - 只有包含 `.md` 文件的文件夹才会在网站上展示

4. **文件夹层级不要过深**：
   - 建议不超过 5 层嵌套
   - 过深的层级会影响用户体验

### 2.4 排除文件/目录

在 `quartz.config.ts` 的 `ignorePatterns` 中配置：

```typescript
ignorePatterns: [
  "private",           // 私有文件
  "xx-归档",           // 归档目录（不会发布）
  "xx-草稿",           // 草稿目录（不会发布）
  ".obsidian",         // Obsidian 配置（不会发布）
  "node_modules",      // Node 依赖（不会发布）
  ".git",              // Git 配置（不会发布）
],
```

**如何添加新的排除规则**：

1. 编辑 `quartz.config.ts` 文件
2. 在 `ignorePatterns` 数组中添加文件夹名称
3. 保存文件
4. 重新构建（`npm run build`）

**示例**：
```typescript
ignorePatterns: [
  "private",
  "xx-归档",
  "xx-草稿",
  "temp",              // 添加临时文件夹
  "draft",             // 添加草稿文件夹
  "backup",            // 添加备份文件夹
  ".obsidian",
  "node_modules",
  ".git",
],
```

### 2.5 双链格式支持

#### Wikilink 格式（推荐）

```markdown
[[文件名]]                    # 基本链接
[[文件名|显示文本]]            # 自定义显示文本
[[文件夹/文件名]]              # 子目录文件
[[文件名#锚点]]                # 锚点链接
```

#### Markdown 链接格式

```markdown
[显示文本](./文件名.md)          # 相对路径
[显示文本](./文件夹/文件名.md)    # 子目录文件
[显示文本](./文件名.md#锚点)      # 锚点链接
```

**注意**：
- 两种格式都支持，建议统一使用一种格式
- 如果文件不存在，链接会显示为红色或无法预览

### 2.6 添加新笔记

1. **创建 Markdown 文件**：
   ```bash
   # 在合适的目录下创建文件
   touch 00-前端体系/01-基础入门/新笔记.md
   ```

2. **编辑内容**：
   ```markdown
   # 新笔记标题
   
   这是笔记内容。
   
   ## 相关链接
   
   - [[相关笔记]]           # 使用双链
   - [相关笔记](./相关笔记.md)  # 或使用 Markdown 链接
   ```

3. **更新 MOC 文件**：
   在对应的 MOC 文件中添加链接：
   ```markdown
   - [新笔记](./01-基础入门/新笔记.md) — 笔记描述
   ```

---

## 三、本地调试

### 3.1 安装依赖

```bash
# 首次安装
npm install

# 或使用脚本（如果存在）
./安装依赖.sh
```

### 3.2 启动开发服务器

```bash
# 启动开发服务器（支持热更新）
npm run dev
```

然后在浏览器中打开 `http://localhost:8080` 查看效果。

### 3.3 开发服务器功能

- ✅ **热更新**：修改文件后自动刷新
- ✅ **实时预览**：修改立即看到效果
- ✅ **错误提示**：在终端显示构建错误
- ✅ **快速调试**：方便本地测试

### 3.4 构建静态文件

```bash
# 构建生产版本
npm run build
```

构建后的文件会在 `public/` 目录中。

### 3.5 预览构建结果

```bash
# 预览构建结果（需要先构建）
npm run preview
```

### 3.6 清理缓存

如果遇到奇怪的问题，可以清理缓存：

```bash
# 删除构建缓存
rm -rf .quartz-cache/
rm -rf public/

# 重新构建
npm run build
```

---

## 四、部署到 GitHub Pages

### 4.1 首次部署

#### 步骤 1：提交代码

```bash
# 添加所有文件
git add .

# 提交
git commit -m "Initial commit: Add Quartz configuration"

# 推送到 GitHub
git push origin master
```

#### 步骤 2：启用 GitHub Pages

1. 进入 GitHub 仓库的 **Settings** → **Pages**
2. 在 **Source** 中选择 **GitHub Actions**
3. 点击 **Save**

#### 步骤 3：等待自动部署

1. 进入 **Actions** 标签页
2. 查看部署进度
3. 等待部署完成（通常 3-5 分钟）

#### 步骤 4：访问网站

部署完成后，访问：`https://你的用户名.github.io/Muliminty-Note/`

### 4.2 自定义域名（可选）

#### 步骤 1：创建 CNAME 文件

创建 `content/CNAME` 文件（如果使用 GitHub Actions）或根目录的 `CNAME` 文件：

```
yourdomain.com
www.yourdomain.com
```

#### 步骤 2：配置 DNS 解析

在域名注册商处配置 DNS：

**CNAME 记录**（推荐）：
```
@ → CNAME → 你的用户名.github.io
www → CNAME → 你的用户名.github.io
```

**A 记录**（根域名）：
```
@ → A → 185.199.108.153
@ → A → 185.199.109.153
@ → A → 185.199.110.153
@ → A → 185.199.111.153
www → CNAME → 你的用户名.github.io
```

#### 步骤 3：在 GitHub 中配置

1. 进入仓库 **Settings** → **Pages**
2. 在 **Custom domain** 中输入你的域名
3. 等待 DNS 生效后，勾选 **Enforce HTTPS**

### 4.3 HTTPS 配置

GitHub Pages 会自动为所有域名提供 HTTPS 证书（Let's Encrypt）。

**启用步骤**：
1. 等待 DNS 配置生效
2. 进入仓库 **Settings** → **Pages**
3. 勾选 **Enforce HTTPS**
4. 等待证书生成（通常几分钟）

---

## 五、更新笔记

### 5.1 日常更新流程

#### 方法一：使用 Git 命令（推荐）

```bash
# 1. 编辑笔记文件（使用 Obsidian 或其他编辑器）

# 2. 查看更改
git status

# 3. 添加更改的文件
git add .

# 或只添加特定文件
git add 00-前端体系/01-基础入门/新笔记.md

# 4. 提交更改
git commit -m "更新：添加新笔记"

# 5. 推送到 GitHub
git push origin master
```

#### 方法二：使用 Obsidian Git 插件

如果安装了 Obsidian Git 插件：

1. 编辑笔记文件
2. 点击 **Commit** 按钮（或使用快捷键）
3. 插件会自动提交并推送

### 5.2 批量更新

如果更新了多个笔记：

```bash
# 查看所有更改
git status

# 添加所有更改
git add .

# 提交（使用描述性的提交信息）
git commit -m "更新：添加多个笔记和更新内容"

# 推送
git push origin master
```

### 5.3 更新后自动部署

推送代码后，GitHub Actions 会自动：
1. 检测到代码更改
2. 触发构建流程
3. 重新构建网站
4. 部署到 GitHub Pages

**无需手动操作**，等待 3-5 分钟即可看到更新。

---

## 六、触发重新构建

### 6.1 自动触发

以下情况会自动触发重新构建：

- ✅ **推送代码**：`git push origin master`
- ✅ **合并 Pull Request**：合并 PR 到 master 分支
- ✅ **手动触发**：在 GitHub Actions 中手动触发

### 6.2 手动触发

#### 方法一：通过 GitHub 界面

1. 进入 GitHub 仓库的 **Actions** 标签页
2. 选择 **Deploy Quartz site to GitHub Pages** 工作流
3. 点击 **Run workflow** 按钮
4. 选择分支（通常是 `master`）
5. 点击 **Run workflow**

#### 方法二：通过 Git 命令

```bash
# 创建一个空提交来触发构建
git commit --allow-empty -m "Trigger rebuild"
git push origin master
```

#### 方法三：修改 `.github/workflows/deploy.yml`

添加 `workflow_dispatch` 触发器（如果还没有）：

```yaml
on:
  push:
    branches:
      - master
  workflow_dispatch:  # 允许手动触发
```

### 6.3 查看构建状态

1. 进入 **Actions** 标签页
2. 查看最新的工作流运行
3. 点击查看详细日志
4. 检查是否有错误

### 6.4 构建失败处理

如果构建失败：

1. **查看错误日志**：
   - 进入 **Actions** 标签页
   - 点击失败的工作流
   - 查看错误信息

2. **常见错误**：
   - **依赖安装失败**：检查 `package.json` 是否正确
   - **构建脚本错误**：检查 `quartz.config.ts` 配置
   - **文件路径错误**：检查文件路径是否正确

3. **修复后重新提交**：
   ```bash
   # 修复错误后
   git add .
   git commit -m "修复构建错误"
   git push origin master
   ```

---

## 七、配置说明

### 7.1 基本配置

编辑 `quartz.config.ts`：

```typescript
configuration: {
  pageTitle: "Muliminty Note",        // 网站标题
  enableSPA: true,                      // 启用单页应用模式
  enablePopovers: true,                // 启用链接预览（双链悬浮预览）
  locale: "zh-CN",                     // 语言设置
  baseUrl: "你的用户名.github.io",      // 网站基础 URL
  ignorePatterns: [                     // 忽略的文件/目录
    "private",
    "xx-归档",
    "xx-草稿",
    ".obsidian",
  ],
}
```

### 7.2 主题配置

```typescript
theme: {
  fontOrigin: "googleFonts",           // 字体来源
  cdnCaching: true,                     // 启用 CDN 缓存
  typography: {
    header: "Schibsted Grotesk",        // 标题字体
    body: "Source Sans Pro",            // 正文字体
    code: "IBM Plex Mono",              // 代码字体
  },
  colors: {
    lightMode: {
      // 浅色主题颜色
    },
    darkMode: {
      // 深色主题颜色
    },
  },
}
```

### 7.3 布局配置

编辑 `quartz.layout.ts`：

```typescript
// 左侧边栏组件
const left: Component.ComponentId[] = [
  Component.PageTitle(),                // 页面标题
  Component.Search(),                   // 搜索框
  Component.Darkmode(),                // 深色模式切换
  Component.Explorer({                  // 文件目录浏览器
    title: "📁 目录",
    folderClickBehavior: "collapse",
    folderDefaultState: "collapsed",
  }),
]

// 右侧边栏组件
const right: Component.ComponentId[] = [
  Component.TableOfContents({          // 文章目录
    title: "📑 目录",
  }),
  Component.Backlinks({                // 反向链接
    title: "反向链接",
  }),
]
```

### 7.4 插件配置

在 `quartz.config.ts` 中配置插件：

```typescript
plugins: {
  transformers: [
    Plugin.ObsidianFlavoredMarkdown({  // Obsidian 格式支持
      enableInHtml: true,
      wikilinks: true,
      callouts: true,
      mermaid: true,
    }),
    Plugin.TableOfContents(),          // 目录插件
    // ... 其他插件
  ],
  emitters: [
    Plugin.Search(),                    // 搜索插件
    // ... 其他插件
  ],
}
```

### 7.5 功能配置说明

#### 📑 目录功能（Table of Contents）

**配置位置**：`quartz.config.ts` → `transformers` → `Plugin.TableOfContents()`

**功能说明**：
- ✅ 自动生成文章目录
- ✅ 显示在右侧边栏（桌面端）
- ✅ 显示在移动端页面顶部
- ✅ 支持多级标题（最多 6 级）
- ✅ 点击目录项可跳转到对应章节

**配置选项**：
```typescript
Plugin.TableOfContents({
  minEntries: 1,        // 最少条目数（1 个标题就显示目录）
  maxDepth: 6,         // 最大深度（支持 6 级标题）
  collapseByDefault: false,  // 默认不折叠
  showByDefault: true,       // 默认显示
})
```

#### 🔍 搜索功能（Full Text Search）

**配置位置**：
- `quartz.config.ts` → `emitters` → `Plugin.Search()`（生成搜索索引）
- `quartz.layout.ts` → `left` → `Component.Search()`（显示搜索组件）

**功能说明**：
- ✅ 全文搜索所有笔记内容
- ✅ 搜索框显示在左侧边栏顶部
- ✅ 支持中文搜索
- ✅ 实时搜索结果预览
- ✅ 键盘快捷键：`Ctrl/Cmd + K` 快速打开搜索

**使用方式**：
1. 点击左侧边栏顶部的搜索框
2. 或使用快捷键 `Ctrl/Cmd + K`
3. 输入关键词搜索
4. 点击搜索结果跳转到对应页面

#### 📁 文件目录浏览器（Explorer）

**配置位置**：`quartz.layout.ts` → `left` → `Component.Explorer()`

**功能说明**：
- ✅ 显示完整的文件目录结构
- ✅ 支持文件夹折叠/展开
- ✅ 点击文件跳转到对应页面
- ✅ 自动保存展开状态（本地存储）

**配置选项**：
```typescript
Component.Explorer({
  title: "📁 目录",           // 标题
  folderClickBehavior: "collapse",  // 点击文件夹：折叠/展开
  folderDefaultState: "collapsed",  // 默认状态：折叠
  useSavedState: true,              // 保存状态到本地存储
})
```

#### 🗺️ 知识图谱（Graph）

**配置位置**：`quartz.layout.ts` → `left` → `Component.Graph()`

**功能说明**：
- ✅ 可视化笔记之间的链接关系
- ✅ 本地图谱：显示当前页面的链接关系
- ✅ 全局图谱：显示整个知识库的链接关系
- ✅ 支持拖拽和缩放
- ✅ 点击节点跳转到对应页面

#### 🔗 反向链接（Backlinks）

**配置位置**：`quartz.layout.ts` → `right` → `Component.Backlinks()`

**功能说明**：
- ✅ 显示哪些页面链接到当前页面
- ✅ 帮助发现相关笔记
- ✅ 显示在右侧边栏（桌面端）

更多配置说明请参考：
- [Quartz 定制教程](./03-服务端/04-部署与运维/静态网站部署/Quartz定制教程.md)

---

## 八、常见问题

### 8.1 左侧目录和搜索框不显示

**原因**：
- 布局配置未正确导入
- 依赖未安装
- 构建未完成

**解决方案**：

1. **检查布局配置**：
   确保 `quartz.config.ts` 中导入了布局配置：
   ```typescript
   import * as Shared from "./quartz.layout"
   
   const config: QuartzConfig = {
     // ...
     layout: {
       sharedPageComponents: Shared.sharedPageComponents,
       defaultContentPageLayout: Shared.defaultContentPageLayout,
       defaultListPageLayout: Shared.defaultListPageLayout,
     },
   }
   ```

2. **安装依赖**：
   ```bash
   npm install
   ```

3. **重新构建**：
   ```bash
   rm -rf .quartz-cache/ public/
   npm run build
   ```

4. **检查构建输出**：
   查看 `public/` 目录是否生成了文件，如果为空或没有生成，说明构建失败。

5. **查看构建日志**：
   运行 `npm run build` 时查看是否有错误信息。

### 8.2 双链悬浮预览不显示

**原因**：
- `enablePopovers` 未启用
- Obsidian 插件配置不正确
- 链接格式不正确

**解决方案**：

1. **检查配置**：
   确保 `quartz.config.ts` 中：
   ```typescript
   configuration: {
     enablePopovers: true,  // 必须启用
   },
   ```

2. **检查 Obsidian 插件**：
   ```typescript
   Plugin.ObsidianFlavoredMarkdown({
     enableInHtml: true,
     wikilinks: true,      // 启用 wikilinks
     callouts: true,
     mermaid: true,
   }),
   ```

3. **检查链接格式**：
   在 Markdown 文件中使用正确的格式：
   ```markdown
   [[文件名]]                    // ✅ 正确
   [[文件名|显示文本]]            // ✅ 正确
   [文本](./文件名.md)            // ✅ 正确
   
   [[文件名]]                    // ❌ 如果文件不存在，可能无法预览
   ```

4. **清除缓存并重新构建**：
   ```bash
   rm -rf .quartz-cache/ public/
   npm run build
   ```

### 8.3 某些文件没有显示

**原因**：
- 文件在 `ignorePatterns` 中
- 文件路径错误
- 构建失败

**解决方案**：

1. **检查 `ignorePatterns`**：
   确保文件不在忽略列表中

2. **检查文件路径**：
   确保文件路径正确

3. **查看构建日志**：
   ```bash
   npm run build
   ```
   查看是否有错误信息

### 8.4 部署后网站无法访问

**原因**：
- GitHub Pages 未启用
- 构建失败
- 域名配置错误

**解决方案**：

1. **检查 GitHub Pages 设置**：
   - 进入 **Settings** → **Pages**
   - 确保 Source 选择 **GitHub Actions**

2. **查看 Actions 日志**：
   - 进入 **Actions** 标签页
   - 查看构建是否成功

3. **检查域名配置**：
   如果使用自定义域名，检查 DNS 配置

### 8.5 依赖安装失败

**原因**：
- Node.js 版本过低
- 网络问题
- `package.json` 配置错误

**解决方案**：

1. **检查 Node.js 版本**：
   ```bash
   node --version  # 需要 18+
   ```

2. **清理并重新安装**：
   ```bash
   rm -rf node_modules package-lock.json
   npm install
   ```

3. **使用国内镜像**（如果网络慢）：
   ```bash
   npm install --registry=https://registry.npmmirror.com
   ```

### 8.6 构建失败

**原因**：
- 配置文件语法错误
- 依赖问题
- 文件路径错误

**解决方案**：

1. **检查配置文件语法**：
   - `quartz.config.ts`
   - `quartz.layout.ts`

2. **查看详细错误**：
   ```bash
   npm run build -- --verbose
   ```

3. **查看 GitHub Actions 日志**：
   - 进入 **Actions** 标签页
   - 查看详细的错误信息

### 8.7 找不到模块错误

#### 错误：找不到模块 "quartz/config"

**原因**：依赖未安装

**解决**：
```bash
npm install
```

#### 错误：找不到模块 "./quartz.layout"

**原因**：布局文件路径错误或文件不存在

**解决**：
1. 确保 `quartz.layout.ts` 文件存在
2. 检查导入路径是否正确

### 8.8 页面显示但功能不工作

**原因**：可能是浏览器缓存问题

**解决**：
1. 清除浏览器缓存
2. 硬刷新（Ctrl/Cmd + Shift + R）
3. 检查控制台是否有 JavaScript 错误

### 8.9 验证配置检查清单

在解决问题前，先检查以下清单：

- [ ] 依赖已安装（`npm install`）
- [ ] `quartz.config.ts` 配置正确
- [ ] `quartz.layout.ts` 配置正确
- [ ] `enablePopovers: true` 已设置
- [ ] `Plugin.ObsidianFlavoredMarkdown()` 配置正确
- [ ] `Plugin.Search()` 已添加到 emitters
- [ ] 布局配置已导入到 `quartz.config.ts`
- [ ] 构建成功（`npm run build`）
- [ ] 浏览器控制台无错误

### 8.10 测试步骤

如果功能不工作，按以下步骤测试：

1. **启动开发服务器**：
   ```bash
   npm run dev
   ```

2. **访问网站**：
   打开 `http://localhost:8080`

3. **检查左侧边栏**：
   - 应该看到搜索框
   - 应该看到文件目录浏览器
   - 应该看到其他组件

4. **测试搜索**：
   - 点击搜索框
   - 输入关键词
   - 应该看到搜索结果

5. **测试双链预览**：
   - 鼠标悬停在 wikilink 上（如 `[[文件名]]`）
   - 应该看到预览窗口

---

## 📚 相关文档

- [Quartz 定制教程](./03-服务端/04-部署与运维/静态网站部署/Quartz定制教程.md) - 详细的配置和定制说明
- [GitHub Pages 部署指南](./03-服务端/04-部署与运维/静态网站部署/GitHub-Pages部署指南.md) - GitHub Pages 详细教程

## 🔧 如果问题仍然存在

如果按照上述步骤仍然无法解决问题：

1. **查看 Quartz 官方文档**：
   - https://quartz.jzhao.xyz/

2. **检查 GitHub Issues**：
   - https://github.com/jackyzha0/quartz/issues

3. **查看示例配置**：
   - https://github.com/jackyzha0/quartz/tree/v4

4. **重新初始化项目**（最后手段）：
   ```bash
   # 备份当前配置
   cp quartz.config.ts quartz.config.ts.bak
   cp quartz.layout.ts quartz.layout.ts.bak
   
   # 重新初始化（可选）
   npx quartz create
   ```

---

## 🎯 快速参考

### 常用命令

```bash
# 安装依赖
npm install

# 本地开发（热更新）
npm run dev

# 构建生产版本
npm run build

# 预览构建结果
npm run preview

# 提交代码
git add .
git commit -m "更新笔记"
git push origin master
```

### 工作流程

1. **编辑笔记** → 使用 Obsidian 或其他编辑器
2. **本地预览** → `npm run dev` 查看效果
3. **提交代码** → `git add . && git commit -m "更新" && git push`
4. **自动部署** → GitHub Actions 自动构建和部署
5. **访问网站** → 等待 3-5 分钟后访问

---

**最后更新**：2025  
**维护者**：请根据实际情况更新此文档

