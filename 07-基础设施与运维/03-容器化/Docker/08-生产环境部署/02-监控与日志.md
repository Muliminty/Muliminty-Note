---
title: "监控与日志"
date: "2026-03-19"
lastModified: "2026-03-19"
tags: ["Docker", "生产环境部署", "监控", "日志"]
moc: "[[!MOC-Docker]]"
stage: "工程化实践"
prerequisites: ["部署策略", "容器资源限制"]
description: "整理 Docker 容器在生产环境中的监控工具、日志收集、性能监控、告警与日志聚合方案。"
publish: true
aliases: ["Docker 监控与日志"]
toc: true
---

# 监控与日志

> Docker 容器监控与日志管理

---

## 📋 目录

- [容器监控工具](#容器监控工具)
- [日志收集](#日志收集)
- [性能监控](#性能监控)
- [告警配置](#告警配置)
- [日志聚合](#日志聚合)

---

## 容器监控工具

### docker stats

```bash
# 实时监控容器资源
docker stats

# 监控指定容器
docker stats my-container
```

### cAdvisor

```bash
# 运行 cAdvisor
docker run -d \
  --name=cadvisor \
  -p 8080:8080 \
  -v /:/rootfs:ro \
  -v /var/run:/var/run:ro \
  -v /sys:/sys:ro \
  -v /var/lib/docker/:/var/lib/docker:ro \
  google/cadvisor:latest
```

---

## 日志收集

### 日志驱动

```bash
# 使用 JSON 文件驱动
docker run --log-driver json-file --log-opt max-size=10m nginx:latest

# 使用 syslog 驱动
docker run --log-driver syslog nginx:latest
```

### 查看日志

```bash
# 查看容器日志
docker logs my-container

# 实时查看日志
docker logs -f my-container
```

---

## 性能监控

### 监控指标

- CPU 使用率
- 内存使用率
- 网络 I/O
- 磁盘 I/O

---

## 告警配置

### Prometheus + Grafana

```yaml
# docker-compose.yml
services:
  prometheus:
    image: prom/prometheus
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml

  grafana:
    image: grafana/grafana
    ports:
      - "3000:3000"
```

---

## 日志聚合

### ELK Stack

```yaml
# docker-compose.yml
services:
  elasticsearch:
    image: elasticsearch:7.14.0

  logstash:
    image: logstash:7.14.0

  kibana:
    image: kibana:7.14.0
    ports:
      - "5601:5601"
```

---

## 📚 参考资源

- [Docker 官方文档 - 日志](https://docs.docker.com/config/containers/logging/)
- [Prometheus 文档](https://prometheus.io/docs/)
- [Grafana 文档](https://grafana.com/docs/)

---

## 相关笔记

- [01-部署策略.md](./01-部署策略.md)
- [03-容器编排方案.md](./03-容器编排方案.md)
- [../09-故障排查/03-故障诊断工具.md](../09-故障排查/03-故障诊断工具.md)
