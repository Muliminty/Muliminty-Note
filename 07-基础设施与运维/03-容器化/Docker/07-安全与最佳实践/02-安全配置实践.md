---
title: "安全配置实践"
date: "2026-03-19"
lastModified: "2026-03-19"
tags: ["Docker", "安全", "安全配置", "实践"]
moc: "[[!MOC-Docker]]"
stage: "工程化实践"
prerequisites: ["安全基础", "Dockerfile 最佳实践"]
description: "整理 Docker 中非 root 用户、只读文件系统、网络隔离、镜像扫描与密钥管理等安全配置做法。"
publish: true
aliases: ["Docker 安全配置实践"]
toc: true
---

# 安全配置实践

> Docker 安全配置方法和实践

---

## 📋 目录

- [非 root 用户运行](#非-root-用户运行)
- [只读文件系统](#只读文件系统)
- [资源限制](#资源限制)
- [网络隔离](#网络隔离)
- [镜像安全扫描](#镜像安全扫描)
- [密钥管理](#密钥管理)

---

## 非 root 用户运行

### 在 Dockerfile 中创建用户

```dockerfile
# 创建非 root 用户
RUN addgroup -g 1000 appuser && \
    adduser -D -u 1000 -G appuser appuser

# 切换到非 root 用户
USER appuser
```

### 运行时指定用户

```bash
# 使用非 root 用户运行
docker run -u 1000:1000 nginx:latest
```

---

## 只读文件系统

### 使用只读根文件系统

```bash
# 只读根文件系统
docker run --read-only nginx:latest

# 只读根文件系统 + tmpfs
docker run --read-only --tmpfs /tmp nginx:latest
```

---

## 资源限制

### 设置资源限制

```bash
# CPU 和内存限制
docker run -d \
  --cpus="1.0" \
  --memory="512m" \
  nginx:latest
```

---

## 网络隔离

### 使用自定义网络

```bash
# 创建隔离网络
docker network create --internal isolated-network

# 使用隔离网络
docker run --network isolated-network nginx:latest
```

---

## 镜像安全扫描

### 扫描镜像

```bash
# Docker Scout
docker scout cves nginx:latest

# Trivy
trivy image nginx:latest
```

---

## 密钥管理

### 使用 Docker Secrets

```bash
# 创建 secret
echo "my-secret" | docker secret create my-secret -

# 在服务中使用
docker service create --secret my-secret nginx:latest
```

---

## 📚 参考资源

- [Docker 官方文档 - 安全配置](https://docs.docker.com/engine/security/)
- [Docker 安全最佳实践](https://docs.docker.com/engine/security/best-practices/)

---

## 相关笔记

- [01-安全基础.md](./01-安全基础.md)
- [03-Docker最佳实践.md](./03-Docker最佳实践.md)
- [../04-Dockerfile高级应用/04-Dockerfile最佳实践.md](../04-Dockerfile高级应用/04-Dockerfile最佳实践.md)
